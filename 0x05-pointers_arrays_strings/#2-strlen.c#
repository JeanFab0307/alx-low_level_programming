#include <string.h>
#include "main.h"

/**
 *_strlen - return the length of a string
 *@s: the string
 *
 *Return: the lenght of s
 */
int _strlen(char *s)
{
	int l;
	const CHAR;

	Cl = strlen(*s);
	return (l);
}
